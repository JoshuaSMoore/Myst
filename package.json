{
  "name": "myst",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd Myst.client && npm i",
    "setup:server": "cd Myst && npm i",
    "start": "node Myst/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  },
  "dependencies": {
    "firebase": "^9.1.2"
  }
}
